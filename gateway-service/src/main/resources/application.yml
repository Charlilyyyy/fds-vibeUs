spring:
  application:
    name: gateway-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

# Routes and JWT filter will be added when wire-up begins.
# Target URIs (local):
#   auth-service  -> http://localhost:8082
#   user-service  -> http://localhost:8083
#   music-service -> http://localhost:8084
