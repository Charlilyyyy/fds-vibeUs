spring:
  datasource:
    url: jdbc:h2:mem:music_service_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

minio:
  endpoint: http://localhost:9000
  public-url: http://localhost:9000
  access-key: test-access
  secret-key: test-secret
  buckets:
    artists: vibeus-artists
    albums: vibeus-albums
    tracks: vibeus-tracks
